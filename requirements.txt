
# Dependencias del proyecto Seaweedexim Network Optimization
# Instalar con:  pip install -r requirements.txt
 
gurobipy>=11.0      # solver de optimizacion (requiere licencia; la academica es gratuita)
pandas>=2.0         # lectura del Excel y manejo de datos
openpyxl>=3.1       # motor para leer/escribir archivos .xlsx (lo usa pandas)
 